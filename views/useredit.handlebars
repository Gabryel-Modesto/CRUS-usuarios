<h1>Editando o usuário: {{user.name}}</h1>
<form action="/users/update" method="POST" class="form">
<input type="hidden" name="id" value="{{user.id}}">
  <div class="form-control">
    <label for="name">Nome:</label>
    <input type="text" name="nameUser" placeholder="Digite o nome do usuário:" value="{{user.name}}"/>
  </div>
  <div class=" form-control">
    <label for="occpation">Profissão:</label>
    <input type="text" name="occpation" placeholder="Digite a profissão:"  value="{{user.occpation}}"/>
  </div>
  <div class="form form-control form-checkbox">
    {{#if user.newsletter}}
    <input type="checkbox" name="newsletter" checked />
    {{else}}
    <input type="checkbox" name="newsletter" />
    {{/if}}
    <label for="occpation">Enviar promoções?</label>
  </div>
  <input type="submit" value="Editar">
</form>